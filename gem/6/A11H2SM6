µo«H¤H: tsaikd.bbs@tsaikd.no-ip.org (¤d¤H±Ù) ¬ÝªO: plan
¼Ð  ÃD: [·s¼W¥\¯à] global hdr bhttpd edit more post visio ºô­¶¤å³¹®æ¦¡
µo«H¯¸: ¦a¤U´cÅ]«° (2005/08/28 Sun 00:34:33)              Updated: 2005/08/28

[·s¼W¥\¯à] global.h hdr.h bhttpd.c edit.c more.c post.c visio.c ºô­¶¤å³¹®æ¦¡

¬Û¨Ì¡G
    [­×¥¿] window.c °T®§µøµ¡¦Û°Ê½Õ¾ã¼e«×¨Ã¥i¤ä´©¦h¦æ°T®§

¥\¯à¡G
    ³o­Ó patch ¥D­nªº¥\¯à¬OÅýÃþ¦ü bbcode ªº»yªk
    ¯à¦b bbs ¤W¤Î bhttpd ¤W¥¿½TÅã¥Ü

¥Î³~¡G
    §Ú¬O¦]¬°·Q¦b bbs ¤W¼g¨Ç¹³ blog ªºªF¦è
    ¥i¬O¦pªG¦³¨Ç¹Ï¤ù·Q©ñ¤W¥h´N¨S¦³¿ìªk¤F
    ©Ò¥H³o½g patch ¥D­n¬OÅý bbs ¸ò web °µ¶i¤@¨Bªº³sµ²
    Åý bbs ¥\¯à§ó±j¤j

»¡©ú¡G
    ¬°¤F¤£§ï¨ì¤@¯ë¯Â¤å¦r¤å³¹ªºÅã¥Ü
    °Ï§O¤@¯ë¤å³¹¸òºô­¶¤å³¹§Ú¬O¥Î¤@­Ó¼ÐÅÒ¨Ó¼Ð°O (POST_HTTP)
    ¦bµo§¹¤å³¹«á¡A«ö Ctrl + W §Y¥i¼Ð°O¬°ºô­¶¤å³¹
    ¦A«ö¤@¦¸´N¦^´_¦¨¤@¯ë¤å³¹

    ¼Ð°O¬°ºô­¶¤å³¹«á
    ­Y¥Î bbs ª½±µÂsÄý
    «h·|´£¿ô¨Ï¥ÎªÌ¨Ï¥Îºô­¶ÂsÄý¥»¤å
    ¨Ãªþ¤Wºô§}

    ¦]¬°³o­Ó patch °Ê¨ì¤F«Ü¦hµ{¦¡
    ¥i¯à·|¦³§Ú¨Sµo²{¨ìªº bug
    ©Î¬O§Úªº¤èªk®Ä¯à«ÜÁV
    ´N½Ð¦U¦ì±jªÌ¤j¤j¦³ªÅªº¸ÜÀ°¦£¤@¤U§a...^_^

½d¨Ò¡G
  example 1:
    in normal bbs:      <img=abc.jpg>¹Ï¤ù»¡©ú</img>
    in patched bbs:     ¹Ï¤ù»¡©ú
    in bhttpd:          ¹Ï¤ù»¡©ú (¤U­±±µ¤F¤@±iabc.jpgªº¹Ï¤ù)

  example 2:
    in normal bbs:      <link=http://abc.def/>¶W³sµ²</link>
    in patched bbs:     ¶W³sµ²
    in bhttpd:          ¶W³sµ² (¥Î·Æ¹«ÂI¤@¤U´N³s¹L¥h¤F)

  example 3:
    in normal bbs:      <lnimg=http://abc.def/,abc.jpg>¶W³sµ²¹Ï¤ù</lnimg>
    in patched bbs:     ¶W³sµ²¹Ï¤ù
    in bhttpd:          ¶W³sµ²¹Ï¤ù (¤U­±±µ¤F¤@±iabc.jpgªº¹Ï¤ù)
                                   (¥Î·Æ¹«ÂI¤@¤U´N³s¹L¥h¤F)
  example site:
    ¦b§Úªº­Ó¤H bbs ¤Wªº­Ó¤HªO¡A¦³¤@¨Ç¬OÄÝ©óºô­¶«¬¤å³¹ªº
    (telnet://tsaikd.no-ip.org) (P_tsaikd)
    ¦³¿³½ìªº¥i¥H¨Ó¬Ý¬Ý...^_^

: global.h

VAR char currtitle[80];                /* currently selected article title */
+ VAR int currxmode;             /* currently selected article xmode */

VAR int currbno;               /* currently selected board bno */
VAR usint currbattr;           /* currently selected board battr */

: global.h

VAR char *str_ransi    INI("\033[m");

+ /* ----------------------------------------------------- */
+ /* BHTTPD ºô­¶«ü¥O³]©w                                   */
+ /* ----------------------------------------------------- */
+
+ #define HTTP_CODENUM    3       /* ºô­¶«ü¥O¼Æ¶q */
+ #define HTTP_CODEMAXLEN 5 + 1   /* ºô­¶«ü¥O³Ìªø¦r¼Æ */
+
+ #ifdef _MAIN_C_
+ VAR char HTML_CODE[HTTP_CODENUM][HTTP_CODEMAXLEN] =
+ {
+   "link", "img", "lnimg"
+ };                              /* ºô­¶«ü¥O¦Cªí */
+ #else
+ VAR char HTML_CODE[HTTP_CODENUM][HTTP_CODEMAXLEN];
+ #endif

#undef VAR
#undef INI

: hdr.h

- #define POST_3         0x00000004
+ #define POST_HTTP      0x00000004      /* ¦³ºô­¶¼Ð°Oªº */

: bhttpd.c:ansi_lnimg() ·s¼W¦b ansi_hyperlink() «á­±

#ifdef HAVE_HYPERLINK
static void
ansi_lnimg(fpw, src)
  FILE *fpw;
  uschar *src;
{
  int ch, mode;
  uschar *textsrc;

  /* tsaikd.050827: ¦]¬°¦¹ tag ¦³¨âºØ¼Ò¦¡¡A©Ò¥H¤ñ¸û³Â·Ð
     mode: 0x00 ªì©l¤Æ
           0x01 ¦³ hyperlink
           0x02 ¦³ image */

  mode = 0x00;
  ch = *src;
  if (ch != ',' && ch != '>') /* §PÂ_¬O§_¦³ link */
    mode |= 0x01;

  /* ³B²z link ªº³¡¥÷ */
  if (mode & 0x01)
  {
    fputs("<a class=PRE target=_blank href=\"", fpw);
    while (ch = *src)
    {
      if (ch < '#' || ch == '<' || ch == '>' || ch > '~' || ch == ',')
        break;
      fputc(ch, fpw);
      src++;
    }
    fputs("\">", fpw);
  }

  /* ¥ý³B²z³Q tag ¥]°_¨Óªº¤å¦r */
  if (ch == ',' || ch=='>')
    textsrc = strstr(src, ">") + 1;
  else
    textsrc = src;
  while (ch = *textsrc)
  {
    if (ch < '#' || ch == '<' || ch == '>')
      break;
    fputc(ch, fpw);
    textsrc++;
  }

  if (*src == ',' && *(src+1) != '>') /* §PÂ_¬O§_¦³ img */
    mode |= 0x02;
  src++;

  /* ³B²z img ªº³¡¥÷ */
  if (mode & 0x02)
  {
    fputs("\n<img src=\"", fpw);
    while (ch = *src)
    {
      if (ch < '#' || ch == '<' || ch == '>' || ch > '~')
        break;
      fputc(ch, fpw);
      src++;
    }
    fputs("\">", fpw);
  }

  /* µ²§À¸É¤W html ªº tag */
  if (mode & 0x01)
    fputs("</a>", fpw);
  if (mode & 0x02)
    fputs("\n", fpw);
}
#endif

: bhttpd.c:ansi_html()

      ansi_tag(fpw);
      continue;
    }

+   /* tsaikd.050821: ¥[¤J¦Û©w¶W³sµ² */
+   int code_len;
+   code_len = strlen("<link>");
+   if (!str_ncmp(src - 1, "<link=", code_len) && strstr(src, "</link>"))
+   {
+     src += (code_len - 1);
+     ansi_lnimg(fpw, src);
+     src = strstr(src, "</link>") + code_len + 1;
+     ch2 = *src;
+    continue;
+   }
+   code_len = strlen("<img>");
+   if (!str_ncmp(src - 1, "<img=", code_len) && strstr(src, "</img>"))
+   {
+     src += (code_len - 2);
+     *src = ',';
+     ansi_lnimg(fpw, src);
+     src = strstr(src, "</img>") + code_len + 1;
+     ch2 = *src;
+     continue;
+   }
+   code_len = strlen("<lnimg>");
+   if (!str_ncmp(src - 1, "<lnimg=", code_len) && strstr(src, "</lnimg>"))
+   {
+     src += (code_len - 1);
+     ansi_lnimg(fpw, src);
+     src = strstr(src, "</lnimg>") + code_len + 1;
+     ch2 = *src;
+     continue;
+   }

    /* ³Ñ¤Uªº¦r¤¸°µhtmlÂà´« */
    if (ch1 == '<')
    {


: edit.c:ansi2n()

          break;
      }
      continue;
    }
+   else if ((currxmode & POST_HTTP) && (ch == '<'))
+   { /* tsaikd.050827: ­pºâºô­¶¦Xªk«ü¥Oªø«× */
+     uschar *tmpstr;
+     int code_index, code_len;
+     uschar code_st[HTTP_CODEMAXLEN + 3], code_end[HTTP_CODEMAXLEN + 3];
+
+     tmpstr = tmp;
+     for (code_index=0 ; code_index<HTTP_CODENUM ; code_index++)
+     {
+       sprintf(code_st, "<%s=", HTML_CODE[code_index]);
+       code_len = strlen(code_st);
+       sprintf(code_end, "</%s>", HTML_CODE[code_index]);
+
+       if (!str_ncmp(tmpstr, code_st, code_len) ||
+         !str_ncmp(tmpstr, code_end, code_len + 1))
+       {
+         while (ch)
+         {
+           ch = *++tmp;
+           if (ch == '>')
+           {
+             tmp++;
+             break;
+           }
+         }
+         break;
+       }
+     }
+   }
    if (ansix <= 0)

: edit.c:n2ansi()

         break;
      }
      continue;
    }
+   else if ((currxmode & POST_HTTP) && (ch == '<'))
+   { /* tsaikd.050827: ­pºâºô­¶¦Xªk«ü¥Oªø«× */
+     uschar *tmpstr;
+     int code_index, code_len;
+     uschar code_st[HTTP_CODEMAXLEN + 3], code_end[HTTP_CODEMAXLEN + 3];
+
+     tmpstr = tmp;
+     for (code_index=0 ; code_index<HTTP_CODENUM ; code_index++)
+     {
+       sprintf(code_st, "<%s=", HTML_CODE[code_index]);
+       code_len = strlen(code_st);
+       sprintf(code_end, "</%s>", HTML_CODE[code_index]);
+
+       if (!str_ncmp(tmpstr, code_st, code_len) ||
+         !str_ncmp(tmpstr, code_end, code_len + 1))
+       {
+         while (ch)
+         {
+           ch = *++tmp;
+           if (ch == '>')
+           {
+             tmp++;
+             break;
+           }
+         }
+         break;
+       }
+     }
+     if ((tmpstr - tmp) > 2)
+       continue;
+   }
    if (tmp >= nxp)
      break;

: more.c:more_line()

  uschar *pool, *base, *tail;
  int ch, len, bytes, in_ansi, in_chi;
+ int in_http;

  pool = more_pool;
  base = pool + more_base;
  tail = base + more_size;

  len = bytes = in_ansi = in_chi = 0;
+ in_http = 0;

  ...
  ...

      if (!strchr(STR_ANSICODE, ch))
       in_ansi = 0;
    }
+   else if ((currxmode & POST_HTTP) && (ch == '<'))
+   { /* tsaikd.050827: ºô­¶«ü¥O¤£ºâ¤JÅã¥Ü¦r¼Æ */
+     uschar *tmpstr;
+     int code_index, code_len;
+     uschar code_st[HTTP_CODEMAXLEN + 3], code_end[HTTP_CODEMAXLEN + 3];
+
+     tmpstr = base - 1;
+     for (code_index=0 ; code_index<HTTP_CODENUM ; code_index++)
+     {
+       sprintf(code_st, "<%s=", HTML_CODE[code_index]);
+       code_len = strlen(code_st);
+       sprintf(code_end, "</%s>", HTML_CODE[code_index]);
+
+       if ((!str_ncmp(tmpstr, code_st, code_len)) ||
+         (!str_ncmp(tmpstr, code_end, code_len + 1)))
+         in_http = 1;
+     }
+   }
+   else if (in_http)
+   {
+     if (ch == '>')
+       in_http = 0;
+   }
    else if (isprint2(ch))
    {
      len++;

: post.c:post_browse()

      break;
#endif

+   /* tsaikd.050821: «ØÄ³¨Ï¥ÎÂsÄý¾¹¬Ýºô­¶«¬¤å³¹ */
+   currxmode = xmode;
+   if (currxmode & POST_HTTP)
+   {
+     char buf[512];
+
+     sprintf(buf, "¦¹¤å³¹¬°ºô­¶®æ¦¡¡A«ØÄ³¨Ï¥ÎÂsÄý¾¹³sµ²\nhttp://%s:%d/bmore?%s&%d", MYHOSTNAME, BHTTP_PORT, currboard, pos+1);
+     vmsg(buf);
+   }
+
    hdr_fpath(fpath, dir, hdr);

    /* Thor.990204: ¬°¦Ò¼{more ¶Ç¦^­È */

  ...
  ...
    }
    break;
  }
+ currxmode = 0;

  return post_head(xo);

: post.c ·s¼W¥H¤U¨ç¦¡©ó post_help() «e­±

static int
post_http(xo)
  XO *xo;
{
  HDR *hdr;
  int pos, cur;

  pos = xo->pos;
  cur = pos - xo->top;
  hdr = (HDR *) xo_pool + cur;

  hdr->xmode ^= POST_HTTP;
  currchrono = hdr->chrono;

#ifdef HAVE_XYPOST
  rec_put(xo->dir, hdr, sizeof(HDR), xo->key == XZ_XPOST ? \
    hdr->xid : pos, cmpchrono);
#else
  rec_put(xo->dir, hdr, sizeof(HDR), pos, cmpchrono);
#endif

  return post_load(xo);
}


static int
http_edit(xo)
  XO *xo;
{
  HDR *hdr;
  xo_load(xo, sizeof(HDR));
  hdr = (HDR *) xo_pool + (xo->pos - xo->top);

  if (!cuser.userlevel) /* tsaikd.050822: guest ¤£¯à¹ï¨ä¥L guest ªº¤å³¹­×§ïª¬ºA */
    return XO_NONE;
  if (!(bbstate & STAT_BOARD) && strcmp(hdr->owner, cuser.userid)) /* tsaikd.050822: ¬ÝªOÁ`ºÞ¸ò§@ªÌ¥i­×§ïª¬ºA */
    return XO_NONE;

  if (hdr->xmode & POST_HTTP)
    vmsg("¤å³¹ª¬ºA§ï¬°«Dºô­¶®æ¦¡¡I");
  else
    vmsg("¤å³¹ª¬ºA§ï¬°ºô­¶®æ¦¡¡I");

  return post_http(xo);
}


: post.c:post_edit()

  hdr = (HDR *) xo_pool + (xo->pos - xo->top);
+ currxmode = hdr->xmode;

  hdr_fpath(fpath, xo->dir, hdr);

  ...
  ...

+ currxmode = 0;
  /* return post_head(xo); */

: post.c:post_cb[]

+  Ctrl('W'), http_edit,
   Ctrl('P'), post_add,
   Ctrl('D'), post_prune,

: visio.c:outx()

       continue;
      }
    }
+   else if (currxmode & POST_HTTP) /* tsaikd.050827: ¿é¥X®É¡A¤£Åã¥Üºô­¶«ü¥O */
+   {
+     if ((*str == '<') && (strstr(str, ">")))
+     {
+       uschar *tmpstr;
+       int code_index, code_len;
+       uschar code_st[HTTP_CODEMAXLEN + 3], code_end[HTTP_CODEMAXLEN + 3];
+
+       tmpstr = str;
+       for (code_index=0 ; code_index<HTTP_CODENUM ; code_index++)
+       {
+         sprintf(code_st, "<%s=", HTML_CODE[code_index]);
+         code_len = strlen(code_st);
+         sprintf(code_end, "</%s>", HTML_CODE[code_index]);
+
+         if ((!str_ncmp(tmpstr, code_st, code_len)) ||
+             (!str_ncmp(tmpstr, code_end, code_len + 1)))
+         {
+           str = strstr(tmpstr, ">") + 1;
+           ch = *str;
+         }
+       }
+       if (!ch)
+         continue;
+     }
+   }
    outc(ch);
    str++;

--
 [7;30;41m¤d        ¤H         ±Ù    KD !![m [1;31m¦a¤U´cÅ]«° [32mtelnet://tsaikd.no-ip.org[m
 [1;37;44mK[m [1;37;44mD[melete         ±þ±þ±þ [31m±þ¨ì§A²æ¿Ç¤l "©ñ§¾" !![m
 i    ~~ [1;31m¡´[m     [1;5;33m¢e¢d¢c¢b[m       ¡s¡·    ¡s¡·    ¡s¡· §Ö°k°Ú!!
 l    ~~ ¢«¡Ã¡Ã                  ¢«¡t    ¢«¡t    ¢«¡t
 o   ~ ¡þ¡ý                  ..¡t¡r....¡t¡r....¡t¡r[m

--
 [1;43m¢«[46m¢ª[m Or[1mig[30min[m: [41m ¦¨¤j¸ê°T£»¦a¤U´cÅ]«° [36;47m tsaikd.no-ip.org [m
 [1;44m¢©[41m¢¨[m A[1mut[30mho[mr: [1;33mtsaikd [30m±q [31m140.116.103.204 [30mµoªí[m
