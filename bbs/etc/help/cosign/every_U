  ┌───┬──────┬────────────────────────┐
  │按  鍵│ 函      式 │  敘                                        述  │
  ├───┼──────┼────────────────────────┤
  │Ctrl+U│every_U     │切換到使用者名單                                │
  └───┴──────┴────────────────────────┘

    按 Ctrl+U 可以切換到使用者名單，離開使用者名單以後會回到目前所在位置，
  效果和按 Ctrl+Z 再選 (U)使用者 是相同的。

    要注意的是，使用 Ctrl+U 和 Ctrl+Z 的切換次數不能進入太多層。

 =-=-=-=-=-=-=-=-=-=-=-=-=-=-=-=-=-=-=-=-=-=-=-=-=-=-=-=-=-=-=-=-=-=-=-=-=-=
